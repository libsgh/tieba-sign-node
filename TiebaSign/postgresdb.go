package TiebaSign

import (
	_ "github.com/lib/pq"
)

//var Db *gorm.DB

func init() {
	/*var err error
	Db, err = gorm.Open("postgres", "port=5432 host=localhost user=root password=1234 dbname=tb sslmode=disable")
	if err != nil {
		panic(fmt.Sprintf("Got error when connect database, the error is '%v'", err))
	} else {
		fmt.Println("Postgresql数据库连接成功")
	}
	Db.SingularTable(true)*/
}
